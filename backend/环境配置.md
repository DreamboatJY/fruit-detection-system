在项目根目录下执行以下命令创建必要的存储目录：
是backend的上层
如果已经有文件夹，删除所有内容

注意先备份
init_db.sql

删除完后将该文件重新
而且要放入storage/postgres/init目录下

mkdir -p storage/postgres/data
mkdir -p storage/postgres/init
mkdir -p storage/minio/data
mkdir -p storage/minio/config
mkdir -p storage/redis/data

启动容器
建议挂梯子，去美国
这个命令在backend下执行
docker-compose up -d

容器启动成功：
执行该文件检查是否环境配置成功：test_integration.py
